--- tb/tb_viterbi_decoder_basic.v	2026-05-12 23:44:37.424109151 +0000
+++ /mnt/data/banbinhthuongvi_viterbi_lowpower_v4/tb/tb_viterbi_decoder_basic.v	2026-05-13 00:01:36.213347045 +0000
@@ -103,7 +103,7 @@
     // -------------------------------------------------------------------------
     // Wait for done, check exact latency and output data
     //
-    // For v3, o_done is already high when send_frame returns because the
+    // For v4, o_done is already high when send_frame returns because the
     // combinational Viterbi core is captured at the acceptance edge.
     // -------------------------------------------------------------------------
     task automatic wait_done_and_check;
@@ -186,12 +186,12 @@
 
         apply_reset();
 
-        // Smoke tests
+        // Smoke tests for terminated frames: the last two source bits are tail 0s.
         run_case(8'h00);
-        run_case(8'hFF);
-        run_case(8'hAA);
-        run_case(8'h55);
-        run_case(8'h81);
+        run_case(8'hFC);
+        run_case(8'hA8);
+        run_case(8'h54);
+        run_case(8'h80);
         run_case(8'h3C);
 
         $display("============================================================");
